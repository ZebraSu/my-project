import Vue from 'vue'
import Router from 'vue-router'
//import NabBar from '@/components/NabBar'

Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/',
      name: 'App',
      //component: NabBar
    }
  ]
})
